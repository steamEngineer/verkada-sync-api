export async function updateCollection(collectionId, collectionData, apiKey, workspaceId) {
  throw new Error(`postmanSync.js: updateCollection not yet implemented for collection ${collectionId}`);
}
